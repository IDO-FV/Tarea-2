//franco vazquez ian

#include <stdio.h>
#include <math.h>

void main() 
{
    float radio, area, circunferencia;
    const float PI = M_PI;

    printf("Cual es el radio del círculo: ");
    scanf("%f", &radio);

    area = PI * radio * radio;
    circunferencia = 2 * PI * radio;

    printf("Área del círculo: %.2f\n", area);
    printf("Circunferencia del círculo: %.2f\n", circunferencia);

}
