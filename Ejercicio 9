//franco vazquez ian

#include <stdio.h>

void main ()
{
    float km, dias, precio;
    
    printf ("Cual es la distancia en km de ida: ");
    scanf ("%f", &km);
    
    printf ("Cual es el tiempo de estancia en dias?: ");
    scanf ("%f", &dias);
    
    km = km * 2;
    precio = km * 0.23;
    
    if (dias > 7 && km > 800)
    {
        precio = precio * 0.7;
    }
    
    printf("El precio del ticket es: $%.2f\n", precio);
    
}
